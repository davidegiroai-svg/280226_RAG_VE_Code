# Checkpoint M1_TASK_01_DB

**Data:** 2026-03-02T13:57:00
**Status:** SUCCESS

## Cosa è stato completato

### Schema DB aggiornato
- Tabella `chunks`: `embedding vector(768)` (da vector(1536))
- Indice vettoriale: `idx_chunks_embedding_ivfflat` con cosine similarity

### File aggiornati
- `scripts/db_init.sql`
- `docs/10_run_local.md`
- `_cc_status/checkpoint_status.md`

### Verifica eseguita
```powershell
docker compose down -v
docker compose up --build -d
docker compose exec db psql -U rag -d rag -c "\d chunks"
```

Output verificato:
- embedding: vector(768) ✓
- embedding_model: text ✓
- embedding_dim: int ✓
- indice ivfflat con vector_cosine_ops ✓

## Prossimo task suggerito
**M1_TASK_02_EmbeddingAdapter**
