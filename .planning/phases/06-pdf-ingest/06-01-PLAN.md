---
phase: 06-pdf-ingest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/db_init.sql
  - scripts/migration_m2_pdf.sql
  - api/requirements.txt
autonomous: true
requirements:
  - PDF-01
  - PDF-03

must_haves:
  truths:
    - "La tabella chunks ha le colonne page_start, page_end, section_title dopo l'esecuzione della migration"
    - "Il file db_init.sql include le stesse colonne per nuovi ambienti"
    - "pymupdf4llm>=0.0.17 e' presente in api/requirements.txt"
  artifacts:
    - path: "scripts/migration_m2_pdf.sql"
      provides: "ALTER TABLE idempotente per page_start, page_end, section_title su chunks"
      contains: "ADD COLUMN IF NOT EXISTS page_start"
    - path: "scripts/db_init.sql"
      provides: "Schema completo con colonne PDF per nuovi ambienti"
      contains: "page_start"
    - path: "api/requirements.txt"
      provides: "Dipendenza pymupdf4llm per parsing PDF"
      contains: "pymupdf4llm"
  key_links:
    - from: "scripts/migration_m2_pdf.sql"
      to: "chunks table"
      via: "ALTER TABLE ... ADD COLUMN IF NOT EXISTS"
      pattern: "ADD COLUMN IF NOT EXISTS page_start integer"
    - from: "scripts/db_init.sql"
      to: "chunks table definition"
      via: "colonne aggiuntive nel CREATE TABLE"
      pattern: "page_start integer"
---

<objective>
Predisporre le fondamenta per l'ingest PDF: aggiungere le colonne di paginazione al DB e la dipendenza Python.

Purpose: Il worker che ingesta PDF deve salvare page_start e page_end per ogni chunk. Queste colonne devono esistere nel DB prima di qualsiasi INSERT. La migration e' idempotente (ADD COLUMN IF NOT EXISTS) cosi' un ambiente esistente puo' essere aggiornato senza reset.

Output: migration_m2_pdf.sql (per ambienti esistenti), db_init.sql aggiornato (per nuovi ambienti), pymupdf4llm in requirements.txt.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pdf-ingest/06-CONTEXT.md

<interfaces>
<!-- Struttura attuale della tabella chunks da scripts/db_init.sql -->
<!-- Il CREATE TABLE e' gia' presente — aggiungere solo le colonne mancanti -->

Tabella chunks (colonne attuali):
  id uuid PK
  document_id uuid FK → documents CASCADE
  kb_id uuid FK → knowledge_base CASCADE
  kb_namespace text NOT NULL
  chunk_index int NOT NULL
  testo text NOT NULL
  start_offset int
  end_offset int
  metadata jsonb DEFAULT '{}'
  embedding_model text
  embedding_dim int
  embedding vector(768)
  ingest_date timestamptz DEFAULT now()

Colonne da aggiungere (NON esistono ancora):
  page_start integer NULL
  page_end integer NULL
  section_title text NULL
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creare script migration_m2_pdf.sql</name>
  <files>scripts/migration_m2_pdf.sql</files>
  <action>
Creare il file scripts/migration_m2_pdf.sql con le seguenti istruzioni idempotenti:

```sql
-- Migration M2 — PDF: aggiunge colonne paginazione alla tabella chunks
-- Sicuro da eseguire piu' volte (ADD COLUMN IF NOT EXISTS)

ALTER TABLE chunks ADD COLUMN IF NOT EXISTS page_start integer;
ALTER TABLE chunks ADD COLUMN IF NOT EXISTS page_end integer;
ALTER TABLE chunks ADD COLUMN IF NOT EXISTS section_title text;
```

Il file deve essere eseguito manualmente dall'utente su ambienti esistenti (DB gia' inizializzato con db_init.sql originale). Il commento in testa spiega lo scopo.

NON creare tabelle nuove. NON modificare colonne esistenti. NON aggiungere vincoli (NOT NULL, DEFAULT) — le colonne devono essere nullable perche' solo i chunk PDF le popolano.
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS" /c/Users/D.Giro/280226_RAG_VE_Code/scripts/migration_m2_pdf.sql</automated>
  </verify>
  <done>Il file esiste con 3 istruzioni ALTER TABLE (una per page_start, una per page_end, una per section_title), tutte con IF NOT EXISTS, tutte senza NOT NULL constraint.</done>
</task>

<task type="auto">
  <name>Task 2: Aggiornare db_init.sql e requirements.txt</name>
  <files>scripts/db_init.sql, api/requirements.txt</files>
  <action>
**db_init.sql — aggiungere colonne nel CREATE TABLE chunks:**

Trovare il blocco CREATE TABLE chunks e aggiungere le tre colonne nuove PRIMA della riga `ingest_date`, mantenendo l'ordine e l'indentazione esistente:

```sql
    page_start integer,
    page_end integer,
    section_title text,
```

Il risultato finale del CREATE TABLE chunks deve contenere (nell'ordine):
  ...
  metadata jsonb DEFAULT '{}'::jsonb,
  embedding_model text,
  embedding_dim int,
  embedding vector(768),
  page_start integer,
  page_end integer,
  section_title text,
  ingest_date timestamptz DEFAULT now()

NON modificare nessun altro blocco di db_init.sql. NON cambiare vincoli esistenti. NON riordinare colonne esistenti.

**api/requirements.txt — aggiungere pymupdf4llm:**

Aggiungere in fondo al file la riga:

```
pymupdf4llm>=0.0.17
```

Mantenere il formato esistente (una dipendenza per riga, nessuna virgoletta).
  </action>
  <verify>
    <automated>grep "page_start" /c/Users/D.Giro/280226_RAG_VE_Code/scripts/db_init.sql && grep "pymupdf4llm" /c/Users/D.Giro/280226_RAG_VE_Code/api/requirements.txt</automated>
  </verify>
  <done>db_init.sql contiene le tre colonne page_start, page_end, section_title nel CREATE TABLE chunks. api/requirements.txt contiene pymupdf4llm>=0.0.17.</done>
</task>

</tasks>

<verification>
Verifica complessiva del piano:

1. scripts/migration_m2_pdf.sql esiste e contiene 3 ADD COLUMN IF NOT EXISTS
2. scripts/db_init.sql contiene page_start, page_end, section_title nel CREATE TABLE chunks
3. api/requirements.txt contiene pymupdf4llm>=0.0.17
4. Nessuna colonna modificata nelle tabelle esistenti knowledge_base e documents
</verification>

<success_criteria>
- migration_m2_pdf.sql esiste ed e' idempotente (IF NOT EXISTS ovunque)
- db_init.sql ha le nuove colonne nella tabella chunks (nuovi ambienti pronti)
- api/requirements.txt ha pymupdf4llm>=0.0.17
- Il file migration_m2_pdf.sql puo' essere eseguito su un DB esistente senza errori
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/06-pdf-ingest/06-01-SUMMARY.md`
</output>
