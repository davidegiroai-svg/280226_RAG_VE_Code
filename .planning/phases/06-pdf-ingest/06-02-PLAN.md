---
phase: 06-pdf-ingest
plan: 02
type: tdd
wave: 2
depends_on:
  - "06-01"
files_modified:
  - api/app/ingest_fs.py
  - tests/test_ingest_pdf.py
autonomous: true
requirements:
  - PDF-01
  - PDF-02
  - PDF-03
  - PDF-04

must_haves:
  truths:
    - "Il worker CLI elabora PDF presenti nella stessa cartella di TXT/MD/CSV/JSON senza errori"
    - "Ogni chunk estratto da un PDF ha page_start e page_end corrispondenti al numero pagina sorgente"
    - "Ingestire lo stesso PDF due volte non produce chunk duplicati (UNIQUE kb_id+content_hash)"
    - "Pagine PDF con testo vuoto vengono scartate senza interrompere l'elaborazione"
    - "PDF corrotti o criptati vengono saltati con warning e l'elaborazione continua"
    - "L'output JSON finale include il campo pdfs_processed con il conteggio dei PDF elaborati"
  artifacts:
    - path: "tests/test_ingest_pdf.py"
      provides: "Test suite completa per PDF ingest (TDD RED before GREEN)"
      min_lines: 60
    - path: "api/app/ingest_fs.py"
      provides: "Worker esteso con read_pdf_chunks(), branch PDF in insert_chunks(), .pdf in list_files()"
      contains: "read_pdf_chunks"
  key_links:
    - from: "api/app/ingest_fs.py list_files()"
      to: "pdf files on disk"
      via: "aggiunta .pdf agli ext supportati"
      pattern: '"\\.pdf"'
    - from: "api/app/ingest_fs.py insert_chunks()"
      to: "read_pdf_chunks()"
      via: "branch su source_ext == '.pdf'"
      pattern: "source_ext.*\\.pdf"
    - from: "api/app/ingest_fs.py insert_chunks()"
      to: "chunks table"
      via: "INSERT con page_start e page_end dalle colonne plan 06-01"
      pattern: "page_start.*page_end"
---

<objective>
Implementare il supporto PDF nel worker ingest usando TDD: prima i test (RED), poi l'implementazione (GREEN).

Purpose: Il worker CLI deve poter elaborare file PDF estraendo testo per pagina con pymupdf4llm, salvando page_start/page_end per ogni chunk, con la stessa deduplication e gestione errori degli altri formati.

Output: tests/test_ingest_pdf.py con test completi, api/app/ingest_fs.py esteso con read_pdf_chunks(), branch in insert_chunks(), .pdf in list_files(), pdfs_processed nel JSON summary.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-pdf-ingest/06-CONTEXT.md
@.planning/phases/06-pdf-ingest/06-01-SUMMARY.md

<interfaces>
<!-- Funzioni esistenti in ingest_fs.py da NON modificare -->

def sha256_text(text: str) -> str:
    """Restituisce sha256 hex della stringa in utf-8."""

def read_text_file(p: Path) -> str:
    """NON modificare. Usata solo per TXT/MD/CSV/JSON."""

def chunk_text(text: str, size: int = 1200, overlap: int = 200) -> Iterable[Tuple[int, str]]:
    """NON usare per PDF — solo per formati testo."""

def ensure_kb(cur, namespace: str) -> str:
    """Crea o recupera KB dal DB, ritorna kb_id (str)."""

def upsert_document(cur, kb_id: str, source_uri: str, titolo: str, content_hash: str):
    """Insert con ON CONFLICT DO NOTHING, ritorna (doc_id: str, is_new: bool)."""

def vector_to_str(vec: List[float]) -> str:
    """Converte lista Python in stringa pgvector '[x1,x2,...,xN]' senza spazi."""

def insert_chunks(cur, kb_id, kb_namespace, doc_id, source_path, file_name, text) -> int:
    """ATTUALE — elabora solo testo. Da estendere con parametro source_ext."""

def list_files(root: Path):
    """ATTUALE — solo {'.txt', '.md', '.csv', '.json'}. Da estendere con '.pdf'."""

# Struttura output JSON attuale di main():
{
    "kb": str,
    "path": str,
    "files_found": int,
    "files_read": int,
    "documents_new": int,
    "documents_skipped_existing": int,
    "chunks_inserted": int
    # DA AGGIUNGERE: "pdfs_processed": int
}

<!-- Struttura chunk PDF da pymupdf4llm (da CONTEXT.md) -->
# pymupdf4llm.to_markdown(str(p), page_chunks=True) ritorna una lista di dict:
# page["text"] -> testo della pagina (str)
# page["metadata"]["page"] -> numero pagina (int, 0-indexed o 1-indexed — verificare)

<!-- Struttura INSERT chunks con nuove colonne (da db_init.sql aggiornato dal piano 06-01) -->
INSERT INTO chunks (
    document_id, kb_id, kb_namespace, chunk_index, testo,
    metadata, embedding, embedding_model, embedding_dim,
    page_start, page_end, section_title
) VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)
</interfaces>
</context>

<feature>
  <name>PDF Ingest per pymupdf4llm</name>
  <files>tests/test_ingest_pdf.py, api/app/ingest_fs.py</files>
  <behavior>
    Comportamenti attesi (da testare con mock, EMBEDDING_PROVIDER=dummy, senza DB reale):

    - read_pdf_chunks(p) restituisce lista di dict con chiavi testo, page_start, page_end, section_title
    - read_pdf_chunks(p) scarta pagine con testo vuoto o solo whitespace
    - read_pdf_chunks(p) su PDF corrotto lancia Exception (catturata da chiamante, non da read_pdf_chunks stessa)
    - list_files(root) restituisce anche file .pdf oltre a .txt/.md/.csv/.json
    - insert_chunks() con source_ext='.pdf' usa read_pdf_chunks() e salva page_start/page_end
    - insert_chunks() con source_ext='.txt' usa chunk_text() (comportamento invariato)
    - content_hash per PDF = sha256 della concatenazione di tutti i testi pagina
    - Ingestire lo stesso PDF due volte: secondo ingest ritorna is_new=False (upsert_document invariato)
    - Il JSON summary di main() include pdfs_processed: int

    Pattern mock da usare (come da testing.md):
    - unittest.mock.patch("app.ingest_fs.pymupdf4llm.to_markdown") per simulare output PDF
    - monkeypatch.setenv("EMBEDDING_PROVIDER", "dummy") per embedding deterministico
    - mock psycopg2 connection per test che toccano il DB
  </behavior>
  <implementation>
    Dopo che i test sono in stato RED, implementare in ingest_fs.py:

    1. Aggiungere import try/except per pymupdf4llm in cima al file (import lazy o top-level con try/except):
       try:
           import pymupdf4llm
       except ImportError:
           pymupdf4llm = None

    2. Aggiungere funzione read_pdf_chunks(p: Path) -> list[dict]:
       - Chiama pymupdf4llm.to_markdown(str(p), page_chunks=True)
       - Filtra pagine con testo vuoto (testo.strip() == "")
       - Ritorna lista di {"testo": str, "page_start": int, "page_end": int, "section_title": None}
       - page_start e page_end: usare page["metadata"]["page"] (verificare se 0-indexed — se si', aggiungere +1)
       - NON cattura eccezioni — le eccezioni risalgono al chiamante (main() le gestisce)

    3. Modificare list_files(root): aggiungere ".pdf" agli ext:
       exts = {".txt", ".md", ".csv", ".json", ".pdf"}

    4. Modificare insert_chunks() — aggiungere parametro source_ext: str = "":
       - Se source_ext == ".pdf": branch PDF
         a. Chiama read_pdf_chunks(Path(source_path)) — source_path e' gia' un path
         b. Filtra chunk con testo vuoto
         c. Batch embed dei testi (embed_texts([c["testo"] for c in chunks]))
         d. INSERT con page_start, page_end, section_title per ogni chunk
         e. Meta JSONB include anche page_start, page_end
       - Altrimenti: comportamento attuale con chunk_text() — colonne page_start/page_end/section_title a NULL (non presenti nell'INSERT o passate come None)
       - Nota: la firma INSERT deve essere aggiornata per includere le nuove colonne, passando NULL per chunk non-PDF

    5. Modificare main() — aggiungere gestione PDF nel loop principale:
       - Aggiungere contatore pdfs_processed = 0
       - Rilevare l'estensione del file: source_ext = fp.suffix.lower()
       - Per file .pdf:
         a. try: leggere pages con read_pdf_chunks(fp)
            testo_completo = "".join(p["testo"] for p in pages)
            if not testo_completo.strip(): continue  (PDF vuoto, skip)
            content_hash = sha256_text(testo_completo)
         b. except Exception as e: print warning + continue (skip file problematico)
         c. Se is_new: chiamare insert_chunks() con source_ext=".pdf"
         d. pdfs_processed += 1
       - Per file non-PDF: comportamento attuale (read_text_file + sha256)
       - Aggiungere "pdfs_processed": pdfs_processed nel JSON summary finale

    Vincolo: NON modificare read_text_file(), chunk_text(), ensure_kb(), upsert_document(), vector_to_str().
    Vincolo: psycopg2 SYNC — nessun async.
    Vincolo: vector_to_str() sempre per liste Python → stringa pgvector.
    Vincolo: Json(meta) da psycopg2.extras per JSONB metadata.
  </implementation>
</feature>

<verification>
Verifica TDD completata:

1. RED: pytest tests/test_ingest_pdf.py -v mostra FAILED (test scritti prima dell'implementazione)
2. GREEN: pytest tests/test_ingest_pdf.py -v mostra tutti PASSED
3. Nessuna regressione: pytest tests/ -v mostra tutti i test esistenti ancora PASSED
4. Almeno 5 test distinti in test_ingest_pdf.py (happy path, pagina vuota, PDF corrotto, list_files, JSON summary)

Comandi per verifica (eseguire nel container):
  docker compose exec api pytest tests/test_ingest_pdf.py -v
  docker compose exec api pytest tests/ -v
</verification>

<success_criteria>
- read_pdf_chunks() restituisce list[dict] con testo, page_start, page_end, section_title
- list_files() include .pdf nei pattern supportati
- insert_chunks() ha branch .pdf che usa page_start/page_end nella INSERT SQL
- main() aggiunge pdfs_processed nel JSON output
- PDF corrotti: warning + skip (nessun crash del batch)
- Tutti i test in tests/test_ingest_pdf.py passano
- Nessuna regressione nei test esistenti (test_embedding_dummy.py)
</success_criteria>

<output>
Dopo il completamento, creare `.planning/phases/06-pdf-ingest/06-02-SUMMARY.md`
</output>
